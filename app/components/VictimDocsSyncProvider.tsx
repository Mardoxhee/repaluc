'use client';

import { useEffect } from 'react';

export function VictimDocsSyncProvider({ children }: { children: React.ReactNode }) {
  useEffect(() => {
    return;
  }, []);

  return <>{children}</>;
}
